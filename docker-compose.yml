services:
  postgres:
    image: pgvector/pgvector:pg16

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ragops

    ports:
      - "5432:5432"

  fastapi:
    build: .

    ports:
      - "8000:8000"

    depends_on:
      - postgres